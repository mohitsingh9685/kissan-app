import 'package:flutter/material.dart';

class SellersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Other Sellers")),
      body: Center(child: Text("sellers list")),
    );
  }
}