import 'package:flutter/material.dart';

class EarningPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Earning")),
      body: Center(child: Text("Earning Page")),
    );
  }
}